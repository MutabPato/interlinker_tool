from django.apps import AppConfig


class InterlinkerConfig(AppConfig):
    """Configuration for the interlinker Django app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'interlinker'